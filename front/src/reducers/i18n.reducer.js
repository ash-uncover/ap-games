
const defaultState = {
  lang: null,
  loaded: false,
  loading: false,
  loadingError: null,
  i18n: {}
}

const reducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
